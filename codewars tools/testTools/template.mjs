import { describe, it, expect, showTestsResults } from './cstest.mjs';
const log = console.log;

describe('Basic Tests', () => {
  it('Pass Tests', () => {
    expect().toBe();
  });
  it('Failed Tests', () => {
    expect().toBe();
  });
});

showTestsResults();
